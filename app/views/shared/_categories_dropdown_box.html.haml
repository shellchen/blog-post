.btn-group.nav_item
  %a.btn.btn-small.dropdown-toggle{"data-toggle" => "dropdown", href: ""}
    Category
    %span.caret
  %ul.dropdown-menu
    %li= link_to "Index", categories_path
    %li.divider
    - Category.all.each do |cate|
      %li= link_to "#{cate.name}", category_path(cate)
    - if logged_in?
      %li.divider
      %li
        = link_to new_category_path do
          %i.icon-plus
          Create